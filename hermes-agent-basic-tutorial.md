# Hermes Agent 基礎實務教學
**授課時間：1 小時（60 分鐘）**
**教學重點：查詢特定網站資料 + 監控網站資料**

---

## 課程目標

完成本課程後，你將能夠：
1. 理解 Hermes Agent 的核心架構
2. 用 Hermes Agent 主動查詢特定網站的資料
3. 設定自動化監控任務，主動推播網站變化

---

## 課前準備（5 分鐘）

### 環境確認
- 確認 Hermes Agent 已啟動：`curl http://localhost:8642/health`
- 確認 Telegram 已連線（應收到上線通知）
- 開啟 Control UI：`http://localhost:9119/`

### 今日使用的工具
| 工具 | 功能 |
|------|------|
| `browser_navigate` | 開啟特定網頁 |
| `browser_snapshot` | 截取畫面文字 |
| `browser_vision` | 用視覺分析網頁 |
| `web_search` | 搜尋網路資料 |
| `cronjob` | 設定自動監控任務 |

---

## 第一部分：認識 Hermes Agent（10 分鐘）

### 什麼是 Hermes Agent？
Hermes Agent 是一套在本機運行的 AI Agent 閘道器，透過 Docker 部署，連接多種訊息平台（Telegram、Discord...）與多種工具（瀏覽器、終端機、檔案系統...）。

### 核心架構
```
Telegram / Discord / ...
        ↓
  Hermes Gateway（8642 端口）
        ↓
   Agent（大腦）← 你在對話的 AI
        ↓
   Tools（手）← 瀏覽器、終端機、檔案、搜尋...
```

### 兩種啟動方式差異
| 安裝類型 | 端口 | 特色 |
|----------|------|------|
| Docker 容器 | 8642 | 一鍵啟動、隔離環境 |
| 本機安裝 | 18789 | 直接訪問主機檔案 |

### 教學互動：確認你的 Agent 上線了
```
學員操作：傳送「你好」到 Telegram Bot
預期回應：Hermes Agent 用中文回覆
```

---

## 第二部分：查詢特定網站資料（25 分鐘）

### 2-1：用瀏覽器工具開網頁（10 分鐘）

**老師示範：查台積電股價**
```
學員操作：問 Hermes Agent「幫我查一下台積電股價」
Agent 回覆：用 browser_navigate 開啟 Yahoo Finance
```

為什麼用瀏覽器而不是 web_fetch？
- 動態載入的內容（JS 渲染）需要瀏覽器
- 需要截圖、視覺確認
- 登入後才能看到的資料

**實作任務 1：查單一網站**
```
主題：查 PChome 首頁標題新聞
工具：browser_navigate + browser_snapshot
```

### 2-2：用 web_search 主動搜尋（8 分鐘）

**何時用 web_search：**
- 不知道目標網址
- 需要多個來源的結果
- 快速概覽而非深度瀏覽

**老師示範：搜尋「OpenClaw 最新消息」**
```
學員操作：「幫我搜尋 OpenClaw 最新版本功能」
```

**實作任務 2：專題研究**
```
主題：用 web_search 找「2026 年 AI Agent 趨勢」
工具：web_search + 限縮來源（Reuters、BBC）
```

### 2-3：截圖視覺分析（7 分鐘）

**browser_vision 的應用場景：**
- CAPTCHA 驗證碼識別
- 圖表、發票、截圖解讀
- 需要視覺確認的自動化流程

**老師示範：對財經網站截圖並解讀圖表**

**實作任務 3：視覺解析**
```
主題：截圖任意新聞首頁，說出前三則新聞標題
工具：browser_navigate + browser_vision
```

---

## 第三部分：監控網站資料（20 分鐘）

### 3-1：認識 Cron Job 機制（5 分鐘）

Hermes Agent 的 Cron Job 就是「定時自動執行的任務」。

**核心概念：**
- `schedule`：何時執行（cron 語法）
- `prompt`：執行什麼（任務描述）
- `deliver`：結果送到哪（Telegram）

**語法對照：**
| 用途 | 寫法 |
|------|------|
| 每小時 | `0 * * * *` |
| 每天早上 9 點 | `0 9 * * *` |
| 每 30 分鐘 | `*/30 * * * *` |
| 亞洲時區 | 自動套用 Asia/Taipei |

### 3-2：建立第一個監控任務（10 分鐘）

**老師示範：每小時監控台股加權指數**
```
任務：每小時自動查 Yahoo Finance 台股代碼
結果：用 Cron Job 設定每小時執行
推播：結果送到 Telegram
```

**學員實作：建立自己的第一個 Cron Job**
```
主題：監控指定網站是否有更新
工具：cronjob 工具
時間：每小時
推播：Telegram
```

### 3-3：複雜監控：條件觸發（5 分鐘）

**只在新資料出現時推播：**
```python
# 概念說明（非程式碼）
如果這次查到的資料 ≠ 上次的資料
  → 推播給我
否則
  → 安靜，不說話
```

**實作任務 4：設定股價異動監控**
```
主題：設定當台積電股價變動超過 3% 時主動通知
工具：cronjob + 簡單比對邏輯
```

---

## 第四部分：總結與 Q&A（5 分鐘）

### 今日所學
| 技能 | 工具 |
|------|------|
| 開啟任意網頁 | `browser_navigate` |
| 取得頁面文字 | `browser_snapshot` |
| 視覺解讀截圖 | `browser_vision` |
| 搜尋網路資料 | `web_search` |
| 定時自動監控 | `cronjob` |

### 課後練習建議
1. **每日晨報**：設定早上 8 點自動抓取三則科技新聞推播
2. **價格監控**：選一個電商商品，設定漲跌提醒
3. **天氣關注**：設定每天早上提醒特定城市天氣

### 參考資源
- Hermes Agent 文件：`http://localhost:9119/docs`
- Cron 語法：`https://crontab.guru/`

---

## 教學流程總覽（60 分鐘）

| 時間 | 環節 | 內容 |
|------|------|------|
| 0-5 分鐘 | 課前準備 | 環境確認、工具說明 |
| 5-15 分鐘 | 認識 Hermes | 架構、工具、兩種模式 |
| 15-40 分鐘 | 查詢網站 | 瀏覽器、搜尋、視覺分析 |
| 40-55 分鐘 | 監控任務 | Cron Job、條件觸發 |
| 55-60 分鐘 | 總結 Q&A | 成果驗收、資源分享 |
